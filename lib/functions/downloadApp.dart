// import 'package:url_launcher/url_launcher.dart';

// downloadApp(String downloadURL) async {
//   final Uri url = Uri.parse(downloadURL);
//   if (!await launchUrl(url)) {
//     throw Exception('Could not download the app.');
//   }
// }
